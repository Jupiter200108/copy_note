<h1><%= @folder.name %></h1>
<div>
  <% @memos.each do |memo| %>
    <%= link_to memo_path(memo.id), data: {"turbolinks" => false} do %>
      <div class= "text-dark"><%= memo.title %></div>
    <% end %>
  <% end %>
</div>

