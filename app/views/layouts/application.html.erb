<!DOCTYPE html>
<html>
  <head>
    <title>EasyCopy</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://kit.fontawesome.com/bdb2f01ccb.js" crossorigin="anonymous"></script>
  </head>

  <body>
    <header class="bg-info">
      <nav class="navbar navbar-expand-lg navbar-light border-bottom px-0">
        <div class="navbar-brand mx-0">
          EasyCopy
        </div>
        <button class="navbar-toggler navbar-toggler--right" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon text-dark"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
          <ul class="navbar-nav ml-auto">
            <li class="nav-item">
              <% if user_signed_in? %>
              <a class="new-form-trigger" href="#">
              <i class="fa-solid fa-folder-open text-dark mr-1"></i><span class = "mr-3 text-dark">フォルダ</span>
              </a>
                <%= link_to folders_path,class: "text-decoration-none" do %>
                  <i class="fa-solid fa-folder-open text-dark mr-1"></i><span class = "mr-3 text-dark">フォルダ</span>
                <% end %>

                <%= link_to new_memo_path,class: "text-decoration-none" do %>
                  <i class="fa-solid fa-file-pen text-dark mr-1"></i></i><span class = "mr-3 text-dark">新規メモ</span>
                <% end %>

                <%= link_to destroy_user_session_path, method: :delete,class: "text-decoration-none" do %>
                  <i class="fas fa-door-open text-dark mr-1"></i><span class = "mr-3 text-dark">ログアウト</span>
                <% end %>
              <% end %>
            </li>
          </ul>
        </div>
      </nav>
    </header>
    <main>
      <%= yield %>
    </main>
    <div class="new-form">
    <div class="close-icon-area">
      <div class="close-icon">
        <span></span>
        <span></span>
      </div>
    </div>
    <div class = "form col-md-5 col-xl-4 mx-auto">
      <div class="col-md-12 border rounded-lg bg-white text-dark py-4 px-2 my-1">
        <div class="text-center py-4">
          <h3>フォルダ追加</h3>
        </div>
        <%= form_with model: @folder,url: folders_path,method: :post do |f| %>
          <div class="row col-md-12 mx-0">
            <div class="col-5 pl-0"><%= f.label :name,"フォルダ名" %></div>
            <div class="col-7 px-0"><%= f.text_field :name, class: "col-12 px-1" %></div>
          </div>

          <%= f.submit "新規登録", class: "btn btn-block mybtn btn-primary col text-white tx-tfm rounded-pill mt-4" %>
          <div class="closed-form-btn btn btn-block mybtn btn-secondary col text-white tx-tfm rounded-pill">キャンセル</div>
        <% end %>
      </div>
    </div>
    </div>
  </body>
</html>
